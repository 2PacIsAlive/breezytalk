var uuid = require('..')
  , assert = require('assert');

describe('uuid', function(){
  it('should test', function(){
    assert('string' === typeof uuid());
    assert(5 === uuid().split('-').length);
    assert(36 === uuid().length);
  });
});
